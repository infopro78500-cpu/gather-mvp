# iOS (Capacitor)

This directory hosts the iOS project generated by Capacitor. Run `npm run cap:ios` after installing dependencies to build and open the native project. If the project files are missing, execute `npm run cap:sync` followed by `npm run cap:open:ios` to regenerate them once @capacitor/ios is installed.
