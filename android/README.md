# Android (Capacitor)

This directory hosts the Android project generated by Capacitor. Run `npm run cap:android` after installing dependencies to build and open the native project. If the project files are missing, execute `npm run cap:sync` followed by `npm run cap:open:android` to regenerate them once @capacitor/android is installed.
